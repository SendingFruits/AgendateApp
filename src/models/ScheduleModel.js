class ScheduleModel {

    constructor() {
        this.available = true;
    }
  
    setAvailable(available) { this.available = available; }
    getAvailable(available) { return this.available; }
}
  
export default CustomerModel;